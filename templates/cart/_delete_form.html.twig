<form method="post" >
    <button href="{{ path('app_cart_delete', {'id': cart.id}) }}" onclick="return confirm('Are you sure you want to delete this item?') class="btn"><i class="fa fa-trash" aria-hidden="true"></i></button>
</form>
