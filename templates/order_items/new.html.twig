<!DOCTYPE html>

<title>New OrderItems</title>

{% block body %}
    <h1>Create new OrderItems</h1>

    {{ include('order_items/_form.html.twig') }}

    <a href="{{ path('app_order_items_index') }}">back to list</a>
{% endblock %}
