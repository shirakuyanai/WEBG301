<!DOCTYPE html>

<title>Edit OrderItems</title>

{% block body %}
    <h1>Edit OrderItems</h1>

    {{ include('order_items/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_order_items_index') }}">back to list</a>

    {{ include('order_items/_delete_form.html.twig') }}
{% endblock %}
